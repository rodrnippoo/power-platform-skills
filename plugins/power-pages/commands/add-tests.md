# Add Tests

Read and follow the instructions in `${CLAUDE_PLUGIN_ROOT}/skills/add-tests/SKILL.md`.

**Important**:

- Do not announce steps - just execute them
- Check memory-bank.md for project context first
- Set up both unit tests (Vitest) and E2E tests (Playwright)
- Run tests to verify they pass before completing
