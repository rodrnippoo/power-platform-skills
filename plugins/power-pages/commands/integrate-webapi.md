# Integrate Web API

Read and follow the instructions in `${CLAUDE_PLUGIN_ROOT}/skills/integrate-webapi/SKILL.md`.

**Important**:

- Do not announce steps - just execute them
- Check memory-bank.md for table mapping and publisher prefix
- Create Web API service with CSRF token handling
- Find and replace ALL mock/static data with API calls
- Delete unused mock data files after replacement
- Build and upload after completion
