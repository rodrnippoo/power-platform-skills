# Add Sample Data

Read and follow the instructions in `${CLAUDE_PLUGIN_ROOT}/skills/add-sample-data/SKILL.md`.

**Important**:

- Do not announce steps - just execute them
- Check memory-bank.md for table mapping and publisher prefix
- Insert data in dependency order (parent tables before children)
- Verify data was created successfully
